author:	qodo-code-review
association:	none
edited:	false
status:	none
--
### Qodo reviews are paused for this user.

Troubleshooting steps vary by plan [Learn more →](https://docs.qodo.ai/subscription-plans#what-you%E2%80%99ll-see-when-reviews-are-paused)


**On a Teams plan?**
Reviews resume once this user has a paid seat *and* their Git account is linked in Qodo.
[Link Git account →](https://docs.qodo.ai/subscription-plans#linking-a-git-account)

**Using GitHub Enterprise Server, GitLab Self-Managed, or Bitbucket Data Center?**
These require an Enterprise plan - Contact us
[Contact us →](https://docs.qodo.ai/qodo-support#support)

--
author:	sourcery-ai
association:	none
edited:	true
status:	none
--
<!-- Generated by sourcery-ai[bot]: start review_guide -->

## Reviewer's Guide

Implements a new public posts listing feature end-to-end (backend query + API route + frontend service + Home page UI), updates tests and UI component exports, and cleans up linting/formatting to comply with biome/ruff standards.

#### Sequence diagram for listing public posts from Home page

```mermaid
sequenceDiagram
    actor User
    participant Home
    participant postsApi
    participant apiClient
    participant posts_bp
    participant ListPublicPostsQueryHandler
    participant list_public_posts_query_handler
    participant PostRepository

    User->>Home: Visit /
    Home->>postsApi: listPublicPosts(page, limit)
    postsApi->>apiClient: get /posts/public?page=&limit=
    apiClient->>posts_bp: GET /posts/public

    posts_bp->>posts_bp: ListPublicPostsQuery(page, limit)
    posts_bp->>ListPublicPostsQueryHandler: _get_list_public_posts_query_handler()
    posts_bp->>ListPublicPostsQueryHandler: handle(query)
    ListPublicPostsQueryHandler->>list_public_posts_query_handler: list_public_posts_query_handler(query, PostRepository)

    list_public_posts_query_handler->>PostRepository: list_published(limit, offset)
    list_public_posts_query_handler->>PostRepository: count_published()
    PostRepository-->>list_public_posts_query_handler: posts, total_count

    list_public_posts_query_handler-->>ListPublicPostsQueryHandler: ListPublicPostsResponse
    ListPublicPostsQueryHandler-->>posts_bp: ListPublicPostsResponse
    posts_bp-->>apiClient: JSON { posts, total_count, total_pages, page, limit }
    apiClient-->>postsApi: data
    postsApi-->>Home: { posts, ... }
    Home->>Home: setPosts(posts)
    Home-->>User: Render public posts grid / empty state
```

### File-Level Changes

| Change | Details | Files |
| ------ | ------- | ----- |
| Add backend application/query layer and API route to list published posts with pagination via GET /posts/public. | <ul><li>Introduce ListPublicPostsQuery and ListPublicPostsResponse with validation for page and limit.</li><li>Implement list_public_posts_query_handler to paginate published posts using PostRepository and compute total pages.</li><li>Add ListPublicPostsQueryHandler wrapper and factory, and wire a new /posts/public Flask route that parses query params, calls the handler, serializes posts via to_public_dict, and returns pagination metadata with error handling.</li></ul> | `backend/src/backend/application/queries/list_public_posts_query.py`<br/>`backend/src/backend/application/queries/handlers/list_public_posts_query_handler.py`<br/>`backend/src/backend/api/routes/posts.py` |
| Extend frontend posts API client with a typed listPublicPosts helper for the new public posts endpoint. | <ul><li>Add listPublicPosts method that accepts optional page/limit, builds query params, calls /posts/public, and returns typed pagination data.</li><li>Reuse existing PublicPostResponse type for the posts array.</li></ul> | `frontend/src/services/postsApi.ts` |
| Replace Home page health-check view with a public posts grid that consumes the new API and renders a styled, responsive card layout with empty, loading, and error states. | <ul><li>Switch Home.tsx from using healthService.checkHealth to postsApi.listPublicPosts and manage posts/error state instead of healthData.</li><li>Update loading and error UIs to match a dark themed blog layout and surface backend error messages when present.</li><li>Render an empty-state panel when posts length is zero, otherwise render a responsive grid of cards linking to post detail routes, formatting published_at via date-fns and stripping HTML from html_content for the excerpt.</li></ul> | `frontend/src/pages/Home.tsx` |
| Adjust UI component exports and admin tables to align with linting rules and a clearer public API surface. | <ul><li>Reorder and change named exports for AlertDialog, Alert, and Card components to a normalized, consistent order.</li><li>Remove redundant role attributes and previously added biome-ignore comments from admin table components while preserving required test hooks such as role="status" skeletons.</li><li>Minor tweaks to loading elements and structure in HealthMetrics and table components for consistency.</li></ul> | `frontend/src/components/ui/alert-dialog.tsx`<br/>`frontend/src/components/ui/alert.tsx`<br/>`frontend/src/components/ui/card.tsx`<br/>`frontend/src/components/admin/CommentsTable.tsx`<br/>`frontend/src/components/admin/ErrorLogTable.tsx`<br/>`frontend/src/components/admin/PostsTable.tsx`<br/>`frontend/src/components/admin/UserTable.tsx`<br/>`frontend/src/components/admin/HealthMetrics.tsx` |
| Clean up tests and axios mocks to match updated components/behavior and linting expectations. | <ul><li>Remove a redundant ARIA attribute test from UserTable tests now that table role is implicit.</li><li>Reorder axios mock exports to a consistent order and keep named exports exhaustive.</li><li>Introduce or update Home page unit tests (file touched) to cover loading, success, empty, and error states for public posts.</li></ul> | `frontend/tests/unit/components/admin/UserTable.test.tsx`<br/>`frontend/tests/mocks/axios.ts`<br/>`frontend/tests/unit/Home.test.tsx` |

---

<details>
<summary>Tips and commands</summary>

#### Interacting with Sourcery

- **Trigger a new review:** Comment `@sourcery-ai review` on the pull request.
- **Continue discussions:** Reply directly to Sourcery's review comments.
- **Generate a GitHub issue from a review comment:** Ask Sourcery to create an
  issue from a review comment by replying to it. You can also reply to a
  review comment with `@sourcery-ai issue` to create an issue from it.
- **Generate a pull request title:** Write `@sourcery-ai` anywhere in the pull
  request title to generate a title at any time. You can also comment
  `@sourcery-ai title` on the pull request to (re-)generate the title at any time.
- **Generate a pull request summary:** Write `@sourcery-ai summary` anywhere in
  the pull request body to generate a PR summary at any time exactly where you
  want it. You can also comment `@sourcery-ai summary` on the pull request to
  (re-)generate the summary at any time.
- **Generate reviewer's guide:** Comment `@sourcery-ai guide` on the pull
  request to (re-)generate the reviewer's guide at any time.
- **Resolve all Sourcery comments:** Comment `@sourcery-ai resolve` on the
  pull request to resolve all Sourcery comments. Useful if you've already
  addressed all the comments and don't want to see them anymore.
- **Dismiss all Sourcery reviews:** Comment `@sourcery-ai dismiss` on the pull
  request to dismiss all existing Sourcery reviews. Especially useful if you
  want to start fresh with a new review - don't forget to comment
  `@sourcery-ai review` to trigger a new review!

#### Customizing Your Experience

Access your [dashboard](https://app.sourcery.ai) to:
- Enable or disable review features such as the Sourcery-generated pull request
  summary, the reviewer's guide, and others.
- Change the review language.
- Add, remove or edit custom review instructions.
- Adjust other review settings.

#### Getting Help

- [Contact our support team](mailto:support@sourcery.ai) for questions or feedback.
- Visit our [documentation](https://docs.sourcery.ai) for detailed guides and information.
- Keep in touch with the Sourcery team by following us on [X/Twitter](https://x.com/SourceryAI), [LinkedIn](https://www.linkedin.com/company/sourcery-ai/) or [GitHub](https://github.com/sourcery-ai).

</details>

<!-- Generated by sourcery-ai[bot]: end review_guide -->
--
author:	sourcery-ai
association:	none
edited:	false
status:	commented
--
Hey - I've found 3 issues, and left some high level feedback:

- In `list_public_posts`, the generic `except Exception` block logs and re-raises, which will likely return a non-JSON 500 response and contradicts the docstring that implies a JSON error; consider returning a consistent JSON error payload instead of re-raising.
- On the Home page, the error alert displays `error.message` directly, which may leak internal details from backend errors; consider mapping errors to a user-safe, generic message while still logging the detailed message elsewhere.
- The Home page excerpt logic always appends `...` even for very short `html_content`; you might want to conditionally append the ellipsis only when the content exceeds the truncation length to avoid awkward snippets.

<details>
<summary>Prompt for AI Agents</summary>

~~~markdown
Please address the comments from this code review:

## Overall Comments
- In `list_public_posts`, the generic `except Exception` block logs and re-raises, which will likely return a non-JSON 500 response and contradicts the docstring that implies a JSON error; consider returning a consistent JSON error payload instead of re-raising.
- On the Home page, the error alert displays `error.message` directly, which may leak internal details from backend errors; consider mapping errors to a user-safe, generic message while still logging the detailed message elsewhere.
- The Home page excerpt logic always appends `...` even for very short `html_content`; you might want to conditionally append the ellipsis only when the content exceeds the truncation length to avoid awkward snippets.

## Individual Comments

### Comment 1
<location path="backend/src/backend/api/routes/posts.py" line_range="400-409" />
<code_context>
+@posts_bp.route("/public", methods=["GET"])
</code_context>
<issue_to_address>
**issue (bug_risk):** Unexpected errors are re-raised instead of returning a JSON 500 response, which is inconsistent with other endpoints.

In `list_public_posts`, unexpected exceptions are logged and then re-raised, whereas `get_public_post` logs and returns a JSON `{"error": "An error occurred"}` with a 500 status. Re-raising here will likely produce a generic HTML 500 instead of JSON, breaking consistency for clients of the public posts API. Please return a JSON 500 response here as well to align error semantics with `get_public_post`.
</issue_to_address>

### Comment 2
<location path="frontend/src/pages/Home.tsx" line_range="89-90" />
<code_context>
+                  </CardHeader>
+                  <CardContent className="flex-grow">
+                    <p className="text-slate-400/90 line-clamp-3 leading-relaxed font-light text-[15px]">
+                      {post.html_content
+                        .replace(/<[^>]+>/g, '')
+                        .substring(0, 150)
+                        .trim()}
+                      ...
+                    </p>
+                  </CardContent>
</code_context>
<issue_to_address>
**suggestion (bug_risk):** Excerpt generation always appends ellipsis and doesn’t guard against missing `html_content`.

This calls `post.html_content.replace(...).substring(0, 150)` unconditionally and always appends `...`. If `html_content` is `null`/`undefined`, this will throw, and when the content is shorter than 150 chars you still show an ellipsis even though nothing was truncated. Please guard against falsy `html_content` and only append the ellipsis when the text actually exceeds the limit.

```suggestion
                  <CardContent className="flex-grow">
                    <p className="text-slate-400/90 line-clamp-3 leading-relaxed font-light text-[15px]">
                      {(() => {
                        const rawHtml = post.html_content ?? '';
                        const plainText = rawHtml
                          .replace(/<[^>]+>/g, '')
                          .trim();
                        const limit = 150;

                        if (plainText.length <= limit) {
                          return plainText;
                        }

                        return `${plainText.substring(0, limit).trimEnd()}...`;
                      })()}
```
</issue_to_address>

### Comment 3
<location path="frontend/src/services/postsApi.ts" line_range="228" />
<code_context>
+      page: page ?? 1,
+      limit: limit ?? 20,
+    }
+    const response = await apiClient.get('/posts/public', { params })
+    return response.data
+  },
</code_context>
<issue_to_address>
**suggestion:** The `listPublicPosts` API call isn’t typed, so the response is effectively `any`.

Other API calls use generics on `apiClient.get` (e.g. `get<PublicPostResponse>`) so the response is type-checked. Here, `apiClient.get('/posts/public', ...)` has no generic, so `response.data` isn’t validated against the expected `{ posts, total_count, total_pages, page, limit }` shape. Please define a `ListPublicPostsResponse` type and call `apiClient.get<ListPublicPostsResponse>(...)` to restore type safety.

Suggested implementation:

```typescript
export interface ListPublicPostsResponse {
  posts: PublicPostResponse[]
  total_count: number
  total_pages: number
  page: number
  limit: number
}

  async listPublicPosts(
    page?: number,

```

```typescript
  async listPublicPosts(
    page?: number,
    limit?: number,
  ): Promise<ListPublicPostsResponse> {

```

```typescript
    const params: Record<string, number> = {
      page: page ?? 1,
      limit: limit ?? 20,
    }

    const response = await apiClient.get<ListPublicPostsResponse>('/posts/public', {
      params,
    })
    return response.data
  }

```
</issue_to_address>
~~~

</details>

***

<details>
<summary>Sourcery is free for open source - if you like our reviews please consider sharing them ✨</summary>

- [X](https://twitter.com/intent/tweet?text=I%20just%20got%20an%20instant%20code%20review%20from%20%40SourceryAI%2C%20and%20it%20was%20brilliant%21%20It%27s%20free%20for%20open%20source%20and%20has%20a%20free%20trial%20for%20private%20code.%20Check%20it%20out%20https%3A//sourcery.ai)
- [Mastodon](https://mastodon.social/share?text=I%20just%20got%20an%20instant%20code%20review%20from%20%40SourceryAI%2C%20and%20it%20was%20brilliant%21%20It%27s%20free%20for%20open%20source%20and%20has%20a%20free%20trial%20for%20private%20code.%20Check%20it%20out%20https%3A//sourcery.ai)
- [LinkedIn](https://www.linkedin.com/sharing/share-offsite/?url=https://sourcery.ai)
- [Facebook](https://www.facebook.com/sharer/sharer.php?u=https://sourcery.ai)

</details>

<sub>
Help me be more useful! Please click 👍 or 👎 on each comment and I'll use the feedback to improve your reviews.
</sub>
--
author:	gemini-code-assist
association:	contributor
edited:	false
status:	commented
--
## Code Review

This pull request implements a public posts listing feature on both the backend and frontend. On the backend, a new paginated public query handler and a `/posts/public` GET endpoint have been added. On the frontend, the `Home` page has been updated to fetch and display these public posts instead of the system health status, and its unit tests have been rewritten to match this new behavior. Additionally, various UI components have been cleaned up by removing redundant accessibility attributes and formatting imports/exports. No review comments were provided, so I have no feedback to offer on the review itself.

> [!IMPORTANT]
> The [consumer version of Gemini Code Assist on GitHub](https://developers.google.com/gemini-code-assist/docs/review-repo-code) is being sunset. Starting **June 18, 2026**, new organization installations will be blocked, and all code review activity will officially cease on **July 17, 2026**.
> For more details on the timeline and next steps, please review the [Help Documentation](https://developers.google.com/gemini-code-assist/docs/deprecations/consumer-code-review).
--
